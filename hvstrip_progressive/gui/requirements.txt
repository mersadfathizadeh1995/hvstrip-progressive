# GUI Dependencies for HVSTRIP-Progressive
# Install with: pip install -r requirements.txt

# PySide6 - Qt for Python
PySide6>=6.5.0

# QFluentWidgets - Modern Fluent Design UI components
PyQt-Fluent-Widgets>=1.4.0

# For the main package (if not already installed)
# These are already in the main package requirements but listed here for completeness
numpy>=1.24.0
scipy>=1.10.0
matplotlib>=3.7.0
pandas>=2.0.0
pyyaml>=6.0
