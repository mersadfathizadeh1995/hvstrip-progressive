"""Local configuration template — copy to ``local_config.py`` and edit.

This file ships with the repository as a reference.
``local_config.py`` itself is gitignored so your personal paths
never appear in version control.
"""

# Path to the Geopsy gpell executable (Rayleigh ellipticity engine)
GPELL_PATH = r"C:\Geopsy.org\bin\gpell.exe"          # Windows example
# GPELL_PATH = "/usr/local/bin/gpell"                 # Linux example

# Path to Git Bash (used to invoke gpell on Windows)
GIT_BASH_PATH = r"C:\Program Files\Git\git-bash.exe"  # Windows example
# GIT_BASH_PATH = "/bin/bash"                          # Linux example
